## Export Log

* Exported the cleaned dataset to `data/cleaned/nykaa_brands_clean.csv` for use in later analysis and visualization.
* Created a SQLite database (`nykaa.db`) and exported the cleaned DataFrame as a `brands` table.
* Used `if_exists='replace'` to overwrite the table with the latest cleaned version whenever the notebook is rerun.

